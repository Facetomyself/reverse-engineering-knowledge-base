# fart源码分析以及改进

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2024-01-09
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 源码分析工具： http://aospxref.com/android-13.0.0_r3/ 四个脱壳点： dex加载 mcookie openCommon DexFile构造函数拖壳 classLinker 获取dexFile 加固可能自己实现dex加载，不使用源码函数 dex2oat 编译 解释执行，2oat以后提高效率 不一定走这个流程 小于安卓10 ，10 以后不走这个函数 类的加载和初始化 dexhunter 函数执行过程中…

## 正文

源码分析工具： http://aospxref.com/android-13.0.0_r3/

**四个脱壳点：**

  * dex加载

  * mcookie

  * openCommon

  * DexFile构造函数拖壳

  * classLinker 获取dexFile

  * 加固可能自己实现dex加载，不使用源码函数

  * dex2oat 编译

  * 解释执行，2oat以后提高效率

  * 不一定走这个流程

  * 小于安卓10 ，10 以后不走这个函数

  * 类的加载和初始化

  * dexhunter

  * 函数执行过程中

  * fart execute

  * fart invoke

  * dump codeItem

**源码分析**


          1. inmomorydexclassloader -->几次不同参数重载

      2. basedexclassloader

      3. dex-》dexFile  ->dexElements

      4. 存放在dexPathList中

      5. dexFile 是构建的地方  第一个dexbuffer 第二个 classloader ，

      6. 有个mcookie的参数   和 minternalCookie

      7. mookie就是dexFile的构造函数中 ，返回的值

      8. dexFile:

      9. {

      10. 两个构造方法

      11. mcookie=openDexFile(xxx)

      12.   13. mcookie=openInMemoryDexFiles(xxx)    到native  dalvik_system_Dexfile  ->DexFile_openInMemoryDexFilesNative

      14. 在native中：

      15. 取出来dex地址  计算大小 ，memcpy  到MemMap dex_data中,最后添加到dex_mem_maps 列表

      16. CreateCookieFromOatFileManagerResult  ===> ConvertDexFilesToJavaArray   最后返回jlongarray

      17. mcookie的类型是 jlongarray ，实际上是一个指针

      18.   19.   20. openDexFileFromOat ==>

      21. 构造函数： dex_file_loader  有DexFileLoader::openCommon函数  也是一个脱壳点 ，、

      22. 包括standDexFile  , compackDexFile 两个ifelse  终是调用DexFile实例化

      23. DexFile 这里有bgein size    可以进行拖壳

      24.   25. 拖壳的本质 就是找到dexFile

      26. classLink

      27.   28. }

      29.   30.   31. DexClassLoader

      32. ==>DexPathList  ==> makeDexElements  多个file 文件  loadDexFile ==> opendexfile ==> opendexfileNative

      33. ==》 opendexfilefromoat

      34.   35. openandreadmagic   把dex映射到内存 ，最后返回File  ，


**youpk**


          1. 从内存中dump DEX

      2. 构造完整调用链, 主动调用所有方法并dump CodeItem

      3. 合并 DEX, CodeItem

      4. dex cache 中 在defineClassNative

      5. for (; class_idx < dex_file->NumClassDefs(); class_idx++) {


**fart Execute 整体加固**


          1. static inline JValue Execute(

      2.     Thread* self,

      3.     const DexFile::CodeItem* code_item,

      4.     ShadowFrame& shadow_frame,

      5.     JValue result_register,

      6.     bool stay_in_interpreter = false) REQUIRES_SHARED(Locks::mutator_lock_) {

      7.   8.   9.     if(strstr(shadow_frame.GetMethod()->PrettyMethod().c_str(),"<clinit>"))

      10.     {

      11.         savedexfileByExecute(shadow_frame.GetMethod());

      12.         }

      13.   14.   15. namespace art {

      16.     extern "C" void savedexfileByExecute(ArtMethod* artmethod);

      17.   18. get pid

      19. //找到dexfile  begin 和size

      20. const DexFile* dex_file = artmethod->GetDexFile();

      21. const uint8_t* begin_=dex_file->Begin();  // Start of data.

      22. size_t size_=dex_file->Size();  // Length of data.

      23.   24. // classDef 是类的结构，classdef ，去保存

      25.   26. if(classlistfile>0)

      27.                                             {

      28.                                                 for (size_t ii= 0; ii< dex_file->NumClassDefs(); ++ii)

      29.                                                 {

      30.                                                     const DexFile::ClassDef& class_def = dex_file->GetClassDef(ii);

      31.                                                     const char* descriptor = dex_file->GetClassDescriptor(class_def);

      32.                                                     result=write(classlistfile,(void*)descriptor,strlen(descriptor));

      33.                                                     if(result<0)

      34.                                                     {

      35.                                                         LOG(ERROR) << "ArtMethod::dumpdexfilebyArtMethod,write classlistfile file error";

      36.   37.                                                         }

      38.                                                     const char* temp="\n";

      39.                                                     result=write(classlistfile,(void*)temp,1);

      40.                                                     if(result<0)

      41.                                                     {

      42.                                                         LOG(ERROR) << "ArtMethod::dumpdexfilebyArtMethod,write classlistfile file error";

      43.   44.                                                         }

      45.                                                     }

      46.                                                   fsync(classlistfile);

      47.                                                   close(classlistfile);

      48.   49.                                                 }


**类加载器**


          1. BootClassLoader  系统类

      2. BaseDexClassLoader   是下面三个的父类

      3. PathClassLoader  默认的

      4. DexClassloader  用户自己加入

      5. inMemoryClassLoader 用于内存加载dex

      6.   7. 双亲委派 优先看parent可不可以加载 ，优先父class加载


**fart抽取型**


          1. ClassLoader resultClassloader = null;

      2.         Object currentActivityThread = invokeStaticMethod(

      3.                 "android.app.ActivityThread", "currentActivityThread",

      4.                 new Class[]{}, new Object[]{});

      5.         Object mBoundApplication = getFieldOjbect(

      6.                 "android.app.ActivityThread", currentActivityThread,

      7.                 "mBoundApplication");

      8.   9.   10.         Object loadedApkInfo = getFieldOjbect(

      11.                 "android.app.ActivityThread$AppBindData",

      12.                 mBoundApplication, "info");

      13.         Application mApplication = (Application) getFieldOjbect("android.app.LoadedApk", loadedApkInfo, "mApplication");

      14.         resultClassloader = mApplication.getClassLoader();

      15.   16.   17.       //currentActivityThread

      18.       然后获取 mBoundApplication

      19.       // AppBindData  info 是 loadapk     获取 mApplication   最后获取他的classloader

      20.   21.    public static void fart() {

      22.         ClassLoader appClassloader = getClassloader();   /、loader

      23.   24.         ClassLoader parentClassloader=appClassloader.getParent(); //父classloser

      25.         if(appClassloader.toString().indexOf("java.lang.BootClassLoader")==-1)

      26.         {

      27.             fartwithClassloader(appClassloader);

      28.         }

      29.         while(parentClassloader!=null){

      30.             if(parentClassloader.toString().indexOf("java.lang.BootClassLoader")==-1)

      31.             {

      32.                 fartwithClassloader(parentClassloader);  //一直遍历

      33.   34.             }

      35.   36.             parentClassloader=parentClassloader.getParent();

      37.         }

      38.   39.     } //找到所有的classloser

      40. -  mclassloader    用来记录pathClasslOader

      41.         Object pathList_object = getFieldOjbect("dalvik.system.BaseDexClassLoader", appClassloader, "pathList");

      42.         //pathList 里面有 dexElement ， 里面是dex的loadDexFile

      43.         Object[] ElementsArray = (Object[]) getFieldOjbect("dalvik.system.DexPathList", pathList_object, "dexElements");

      44.   45. //找到所有的ex

      46.   47.         dexFile_fileField = (Field) getClassField(appClassloader, "dalvik.system.DexPathList$Element", "dexFile");

      48.         for (int j = 0; j < ElementsArray.length; j++) {

      49.             Object element = ElementsArray[j];

      50.             Object dexfile = null;

      51.             try {

      52.                 dexfile = (Object) dexFile_fileField.get(element); //到这里就得到dexfile 了

      53.   54. //找到所有的类

      55. getClassNameList  传入一个cookie 就可以获取这个dex 的类的方法

      56. cookie 可以通过 dex file中获取，DexFile.mCookie  实际上openDex的返回值就是mcookie

      57.  Object mcookie = getClassFieldObject(appClassloader, "dalvik.system.DexFile", dexfile, "mCookie");

      58.  或者   Object mInternalCookie = mcookie = getClassFieldObject(appClassloader, "dalvik.system.DexFile", dexfile, "mInternalCookie");

      59. classnames = (String[]) getClassNameList.invoke(dexfile, mcookie);  //都是伪代码

      60.   61. //去调用类的函数

      62. for (String eachclassname : classnames) {

      63.                         loadClassAndInvoke(appClassloader, eachclassname, dumpMethodCode_method);

      64.                     }

      65.   66. //loadClassAndInvoke 里面

      67. resultclass = appClassloader.loadClass(eachclassname);  这样调用不会运行


*我们写一个demo 看一下反射是怎么获取一个类的所有的构造方法和函数的 *

          1. import java.lang.reflect.Constructor;

      2. import java.lang.reflect.Method;

      3.   4. public class ExampleClass {

      5.     ExampleClass() {

      6.         // private constructor

      7.     }

      8.   9.     public ExampleClass(String arg) {

      10.         // public constructor with a parameter

      11.     }

      12.   13.     protected ExampleClass(int arg) {

      14.         // protected constructor with a parameter

      15.     }

      16.   17.     public static void main() {

      18.         Class<?> clazz = ExampleClass.class;

      19.   20.         // 获取所有声明的构造方法

      21.         Constructor<?>[] constructors = clazz.getDeclaredConstructors();

      22.   23.         // 输出构造方法信息

      24.         for (Constructor<?> constructor : constructors) {

      25.             System.out.println("Constructor: " + constructor);

      26.         }

      27.   28.         Method[] methods = clazz.getDeclaredMethods();

      29.         if (methods != null) {

      30.             for (Method m : methods) {

      31.                 System.out.println("Method: " + m);

      32.             }

      33.         }

      34.   35.     }

      36.   37.   38.     public static  void printa(){

      39.         System.out.println("printa");

      40.     }

      41.   42.     public static  void printd(){

      43.         System.out.println("printa");

      44.     }

      45.   46.     public static  void printc(){

      47.         System.out.println("printc");

      48.     }

      49. }


这个是调用函数


          1. public class Main {

      2.     public static void main(String[] args) {

      3.         System.out.println("Hello world!");

      4.         ExampleClass st= new ExampleClass();

      5.         st.main();

      6.     }

      7. }


打印下输出：


          1. Hello world!

      2. Constructor: protected ExampleClass(int)

      3. Constructor: public ExampleClass(java.lang.String)

      4. Constructor: ExampleClass()

      5. Method: public static void ExampleClass.main()

      6. Method: public static void ExampleClass.printd()

      7. Method: public static void ExampleClass.printc()

      8. Method: public static void ExampleClass.printa()


**类的加载和初始化流程**


          1. java的加载class

      2. loadClass(name)  这个方法里面就是调用下面几种方法

      3. findLoadedClassed(name)

      4. findBootStrapClassOrNull(name)

      5. findClass()

      6.   7. loadClass --> 在c层dexFile_defineClassNative

      8. 是给一个名字，然后在所有dex中找

      9. loadMethod ：

      10. codeItemOffset

      11. getDexMethodIndex  得到codeItem的偏移 java的函数和artmethod 关联起来


** *查看java invoke的源码  * **


          1. invoke_method

      2.   3.   4.  const dex::CodeItem* code_item = artmethod->GetCodeItem()

      5.  uint32_t  code_item_len = dex_file->GetCodeItemSize(*code_item);

      6.   codeitem的地址 和长度


**dex重构**


          1. bin sprintf(dexfilepath,"/data/data/%s/koohai/%d_ins_%d.bin",szProcName,size_int,(int)gettidv1());

      2. 里面是函数内容

      3. 对应的ins 填充到dex

      4. 有些dex 可能会被修改，无法填充   ，因此需要把这个替换过去 ， 借鉴youpk 里面源码

      5.   6. 要用到dex fix


dexfix 打包jar包


会生成一个dexfixer-main.jar

java -jar dexifxer-main.jar a.dex a.bin out.dex


**fart的改进**

某些函数体 是另一个加密函数，如果不执行，函数体就还不是最终的函数

一些垃圾类 初始化 会被退出 要跳过一些自定义的类 ===》 使用loadclass 不会被识别到

退出函数放在方法里： ===》 会被运行到 遍历所有类的时候 过滤掉

pathclassloader 加载自身dex 有加固的话 加载的是壳的dex mclassloader 是在pathclassloader

因此要用frida 动态获取到 所有classloader 去调用 fartwithclassloader

动态加载的也可以走execute函数 ， 但是要有构造函数


frida增强脱壳

如果为了和frdai一起 ，就注释掉 activityThread.java


          1.  //add

      2. SaveDex.koohaithread();

      3. //addend


全部用frida 去调用

另外也可以 https://blog.csdn.net/viewz/article/details/114693822


          1. //主动调用 DexClassLoader 加载的dex

      2. Java.perform(function () {

      3.     Java.choose("dalvik.system.DexClassLoader", {

      4.         onMatch: function (instance) {

      5.             console.log(instance)

      6.             try {

      7.                 Java.use("android.app.ActivityThread").kooohaifartwithClassloader(instance);

      8.             } catch (e) {

      9.                 console.log(e);

      10.             }

      11.   12.         },onComplete: function () {

      13.             console.log("heap search complete");

      14.   15.         }

      16.     })

      17.   18. })

      19.   20. // 控制fart 的范围  对函数进行修复

      21.   22.   23. function loadoneclass(classname) {

      24.     Java.perform(function () {

      25. //public static void loadClassAndInvoke

      26. //(ClassLoader appClassloader, String eachclassname, Method dumpMethodCode_method)

      27. //public static ClassLoader getClassloader()

      28.         var appClassloader = Java.use("android.app.ActivityThread").getClassloader();

      29.         console.log("appClassloader->", appClassloader);

      30.         //dumpMethodCode

      31. // private static native void dumpMethodCode(Object m);

      32.         var DexFile = Java.use("dalvik.system.DexFile");

      33.         var Object = Java.use("java.lang.Object");

      34.         var array = Java.array("java.lang.Class", [Object.class]);

      35.         var dumpMethodCode = DexFile.class.getDeclaredMethod("saveMethodCode", array);

      36.         dumpMethodCode.setAccessible(true);//私有函数无法直接调用，需要设置

      37.         console.log("dumpMethodCode->", dumpMethodCode);

      38.         Java.use("android.app.ActivityThread").loadClassAndInvoke(appClassloader, classname, dumpMethodCode);

      39.     })

      40.   41.   42. }

      43.   44. //frida 主动调用 fart

      45.   46. function justfart() {

      47.     Java.perform(function () {

      48.         // Java.use("android.app.ActivityThread").fartthread();

      49.         Java.use("com.koohai.SaveDex").koohaithread();

      50. //import com.koohai.SaveDex;

      51.         // SaveDex.koohaithread();

      52.     })

      53. }


* 通过配置目录来判断是否需要脱壳*

          1.  SaveDex：

      2.         public static boolean shouldSaveDex(String curPackName){

      3. //        _curPkgName=curPackName;

      4.         return new File("/data/local/tmp/koohai",curPackName+"/saveDex").exists();

      5.     }

      6.   7. ========================

      8.     activity_thread.java

      9.   10.         String curPkgName=currentPackageName();

      11.         if (SaveDex.shouldSaveDex(curPkgName)){

      12.             SaveDex.koohaithread();

      13.         }

      14.   15. 指定只脱的类

      16.         /data/local/tmp/koohai",curPackName+"/saveDex/includeClassName.txt

      17. 指定排除的类

      18.         /data/local/tmp/koohai",curPackName+"/saveDex/filterClassName.txt

      19.   20.   21.


**新建文件夹目录来开启脱壳**


          1. cd /data/local/tmp

      2. mkdir -p koohai/com.livelab.cn/SaveDex


应对报错的闪图的问题：

跳过处理这个类


          1. 比如在日志中发现 thread1: com.fewd.aa.model.RouteMeta报错了

      2. 就把他加入到filter 《手动添加》

      3. ==》 有可能是前面触发了垃圾类  另一个类被误伤

      4.   5. echo com.fewd.aa.model.RouteMeta>>filterClassName.txt


因此更推荐的是 指定某些类


          1. 例如 com.sfex开头的类


jadx打开dex报错


          1. 首选项中 右下角 插件  关闭验证

      2. 查看一下要看的类  这时候好多类都是空壳函数

      3. 只要一个前缀  echo com.fewd>>includeClassName.txt

      4. 就加入白名单就好

      5.   6. 如果想过滤掉一些包含在白名单里的类 就把黑名单里的类写的细化一些


**其他加固形式**


          1. dex2c

      2. vmp
