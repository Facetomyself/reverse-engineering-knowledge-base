# 从AST到JSVMP（入坑记录）

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2026-03-15
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 新坑开张，方向是 AST 解混淆 + JSVMP 还原 。 先说 AST 这块，用的是基于 sml2h3/ast_tools 改的框架，主要加了一个改动： 每一步转换后都落地保存 ，这样可以在 WebStorm 里直接选两个文件 Compare，肉眼 diff 检查每一步脚本有没有搞坏原逻辑，调试体验好很多。 优点是可以webstorm选中两个文件compare对比，检查脚本有没有问题。 变量重命名这块用了中文，丑是丑了点，因为有时候总…

## 正文

新坑开张，方向是  ** AST 解混淆 + JSVMP 还原  ** 。

先说 AST 这块，用的是基于 sml2h3/ast_tools 改的框架，主要加了一个改动：  ** 每一步转换后都落地保存  ** ，这样可以在
WebStorm 里直接选两个文件 Compare，肉眼 diff 检查每一步脚本有没有搞坏原逻辑，调试体验好很多。


优点是可以webstorm选中两个文件compare对比，检查脚本有没有问题。

变量重命名这块用了中文，丑是丑了点，因为有时候总有词不达意的，忘了那个文件是干嘛的,解哪一块的混淆。


JSVMP 这边，之前对  ` rs  ` 的理解比较粗糙：知道它是  ` eval  ` 里那一串跑完就行了，没深究过内部。这次想借助 AI，把这段 JS
从入口到出口彻底搞清楚。

上面代码是外层  ` eval  ` 之前的流程，Claude Code 负责改代码和运行，捋到最后的  ` while  ` 大循环就卡住了。

然后我把 facelessJsvmp/dump.js 的还原思路给 Claude 看了以后，它判断两者的 VMP
架构不一样，不能直接套。所以这条路要自己趟。

先跟上 AI 的进度，后续持续更新。这是初步还原的部分截图。


还有就是打通了openclaw自动从语雀到公众号的草稿箱流程，中间也解决了代码的markdown的排版问题，如果有想要了解这个skill的话私聊喔
