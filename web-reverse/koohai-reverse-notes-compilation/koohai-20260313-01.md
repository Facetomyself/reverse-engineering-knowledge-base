# khbox1.2更新

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2026-03-13
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 最近对khbox进行了一些改动，下面是两个站点的使用demo（未测试结果是否可用，仅出结果） bot.sannysoft.com 的fingerprint 然后把js稍微改写一下： 如图 运行main_test.js （这个只针对测试demo文件夹）日志如图。 这个站的上半部分js也有测试，就是经过ai修改后，比较杂乱，就不粘贴 了。 顺利跑通后，用某数的进行了一下测试。未细看是否有新的api需要补。还没仔细看过这个加密，后续会仔细研究…

## 正文

最近对khbox进行了一些改动，下面是两个站点的使用demo（未测试结果是否可用，仅出结果）

##  bot.sannysoft.com  的fingerprint


然后把js稍微改写一下：


如图

运行main_test.js （这个只针对测试demo文件夹）日志如图。

这个站的上半部分js也有测试，就是经过ai修改后，比较杂乱，就不粘贴 了。

顺利跑通后，用某数的进行了一下测试。未细看是否有新的api需要补。还没仔细看过这个加密，后续会仔细研究还原一下，看看具体做了哪些加密。

##  某数环境（仅流程出值）

  1. 把获取环境脚本复制到网站，运行后会自动把环境下下来。


运行完后会自动触发下载。，


我删掉了cookie相关的内容，然后直接run。


##  近期更新

然后说一下近期更新的部分，源码里主要对https://browserleaks.com/ 和https://bot.sannysoft.com/
的一些进行了测试。

  1. arraylike 相关的属性增加


          const arrayLikeClasses = ['PluginArray', 'MimeTypeArray', 'Plugin'];        addon.initialize(filteredConfig, arrayLikeClasses);

有类似array属性的定义，本来c层默认有一些的，但是可能会遗漏就在初始化的时候传进去了。

2.image相关


        tpCanvas: () => {            return new Promise((resolve) => {                try {                    const img = new Image();                    const canvasCtx = document.createElement('canvas').getContext('2d');                    img.onload = () => {                        canvasCtx.drawImage(img, 0, 0);                        resolve(canvasCtx.getImageData(0, 0, 1, 1).data);                    };
                        img.onerror = () => {                        resolve(ERROR);                    };                    img.src = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAAC0lEQVQYV2NgAAIAAAUAAarVyFEAAAAASUVORK5CYII=';                } catch (e) {                    resolve(ERROR);                }            });        },

这是sannysoft的图片检测，在onload 和onerror 的时候 卡了一阵子，解决方案很简单，就是在src设置的时候 就调用onload
函数。避免onload无法被触发的情况。

  3. 音视频等


     "HTMLVideoElement_canPlayType": function(type) {            const t = String(type || '');            if (t.includes('avc1') || (t.includes('mp4') && !t.includes('webm')))                return mediaCapsData.video?.h264 || 'probably';            if (t.includes('vp9'))   return mediaCapsData.video?.vp9  || 'probably';            if (t.includes('vp8') || t.includes('webm')) return mediaCapsData.video?.webm || 'probably';            if (t.includes('theora') || (t.includes('ogg') && !t.includes('audio')))                return mediaCapsData.video?.ogg || 'maybe';            if (t.includes('mpegurl') || t.includes('m3u8')) return mediaCapsData.video?.hls || '';            return '';        },        "HTMLMediaElement_canPlayType": function(type) {            const t = String(type || '');            if (t.includes('avc1') || (t.includes('mp4') && !t.includes('webm')))                return mediaCapsData.video?.h264 || 'probably';            if (t.includes('vp9'))                                  return mediaCapsData.video?.vp9  || 'probably';            if (t.includes('vp8') || t.includes('webm'))           return mediaCapsData.video?.webm || 'probably';            if (t.includes('mpeg') || t.includes('mp3'))           return mediaCapsData.audio?.mp3  || 'probably';            if (t.includes('aac'))                                  return mediaCapsData.audio?.aac  || 'probably';            if (t.includes('opus'))                                 return mediaCapsData.audio?.opus || 'probably';            if (t.includes('vorbis') || (t.includes('ogg') && !t.includes('theora')))                return mediaCapsData.audio?.ogg || 'probably';            if (t.includes('wav'))                                  return mediaCapsData.audio?.wav  || 'probably';            if (t.includes('flac'))                                 return mediaCapsData.audio?.flac || 'probably';            if (t.includes('theora'))                               return mediaCapsData.video?.ogg  || 'maybe';            return '';        },

目前测试下来，node层基本没有什么bug了。


这是当前节点框架源码，已放星球。测试代码运行main_test.js 即可。记得先把node 替换一下。
当前框架结合ai很丝滑的办法是给他一个浏览器的真实函数调用日志，然后让他自己运行自己对比修复。确实丝滑。

编译的版本是v25.2.2-pre ，建议本地用24版本+ ，然后在C:\Program
Files\nodejs（安装路径下的node.exe）备份后直接替换。
