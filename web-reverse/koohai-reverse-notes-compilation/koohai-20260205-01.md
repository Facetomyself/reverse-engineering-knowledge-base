# khbox-6：V0.1 功能实现

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2026-02-05
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 上周的问题，问了一周的ai ，终于被我问出来了。通俗点说原型不一致的原因是，node运行js的时候有个自己的context ,在vm运行js 也有个context ，很可能是addon 运行的context 和vm 里执行js 的context 不一样，导致部分原型对不上。经过一通折腾，替换掉了getCurrentContext。 成果如下： input.js let passCount = 0;let failCount = 0; f…

## 正文

上周的问题，问了一周的ai ，终于被我问出来了。通俗点说原型不一致的原因是，node运行js的时候有个自己的context ,在vm运行js
也有个context ，很可能是addon 运行的context 和vm 里执行js 的context
不一样，导致部分原型对不上。经过一通折腾，替换掉了getCurrentContext。 成果如下：

input.js


    let passCount = 0;let failCount = 0;
    function check(name, condition, actual) {    const icon = condition ? '✅' : '❌';    const status = condition ? 'PASS' : 'FAIL';    console.log(`${icon} [${status}] ${name}`);    if (actual !== undefined) {        console.log(`         → ${actual}`);    }    condition ? passCount++ : failCount++;}
    // ==========================================// 1. 基础环境检查// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 1️⃣  基础环境检查                                                │');console.log('└─────────────────────────────────────────────────────────────────┘');
    console.log("Object.getOwnPropertyNames(Navigator):::", Object.getOwnPropertyNames(Navigator).toString())console.log("Navigator.hasOwnProperty('caller'):::", Navigator.hasOwnProperty('caller'))console.log("Object.getOwnPropertyNames(Function):::", Object.getOwnPropertyNames(Function).toString())const desc = Object.getOwnPropertyDescriptor(window, 'navigator');console.log('Descriptor:', JSON.stringify(desc, null, 2));console.log("window.navigator",window.navigator)console.log("window.navigator.toString()",window.navigator.toString())console.log('=====================begin')console.log( Object.getOwnPropertyDescriptor(window, 'navigator').toString())console.log( Object.getOwnPropertyDescriptor(window, 'navigator').get.toString())

    // console.log(Object.getOwnPropertyDescriptor(ss[0].__proto__.__proto__.__proto__, 'innerHTML').set.toString());check(    'document 对象存在',    typeof document === 'object',    `typeof document = "${typeof document}"`);
    check(    'document 原型链正确',    document.__proto__.constructor.name === 'HTMLDocument',    `constructor.name = "HTMLDocument"`);
    // ==========================================// 2. DOM 查询和操作// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 2️⃣  DOM 查询和操作                                              │');console.log('└─────────────────────────────────────────────────────────────────┘');
    const appDiv = document.getElementById('app');check(    'getElementById 正常工作',    appDiv && appDiv.id === 'app',    `获取到元素: id="app", className="${appDiv?.className}"`);
    const spans = document.getElementsByTagName('span');check(    'getElementsByTagName 返回集合',    spans && spans.length === 2,    `获取到 ${spans?.length} 个 span 元素`);
    const h1 = document.querySelector('h1');check(    'querySelector 正常工作',    h1 && h1.textContent === 'Test Page',    `textContent = "${h1?.textContent}"`);
    // ==========================================// 3. instanceof 检查（关键）// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 3️⃣  instanceof 检查（反检测关键点）                             │');console.log('└─────────────────────────────────────────────────────────────────┘');console.log(spans[0].__proto__)console.log(spans[0].toString())
    console.log(spans[0].__proto__ === HTMLSpanElement.prototype);console.log('------------')console.log(Object.getPrototypeOf(spans[0]) === HTMLSpanElement.prototype);console.log('------------')console.log(HTMLSpanElement.prototype.constructor === HTMLSpanElement);console.log('------------')console.log(spans[0].constructor === HTMLSpanElement);console.log('------------')console.log(spans[0] instanceof HTMLSpanElement);console.log('------------')

    check(    'HTMLCollection instanceof',    spans instanceof HTMLCollection,    `spans instanceof HTMLCollection = true`);
    check(    'HTMLSpanElement instanceof',    spans[0] instanceof HTMLSpanElement,    `spans[0] instanceof HTMLSpanElement = true`);
    check(    'HTMLElement instanceof',    spans[0] instanceof HTMLElement,    `spans[0] instanceof HTMLElement = true`);
    check(    'Element instanceof',    spans[0] instanceof Element,    `spans[0] instanceof Element = true`);
    // ==========================================// 4. 原型链验证// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 4️⃣  原型链验证（反检测关键点）                                   │');console.log('└─────────────────────────────────────────────────────────────────┘');
    const proto1 = spans[0].__proto__;check(    '第一层原型: HTMLSpanElement',    proto1.constructor.name === 'HTMLSpanElement',    `spans[0].__proto__.constructor.name = "HTMLSpanElement"`);
    check(    'HTMLSpanElement.prototype 匹配',    HTMLSpanElement.prototype === proto1,    `HTMLSpanElement.prototype === spans[0].__proto__`);
    const proto2 = proto1.__proto__;check(    '第二层原型: HTMLElement',    proto2.constructor.name === 'HTMLElement',    `spans[0].__proto__.__proto__.constructor.name = "HTMLElement"`);
    check(    'HTMLElement.prototype 匹配',    HTMLElement.prototype === proto2,    `HTMLElement.prototype === spans[0].__proto__.__proto__`);
    // ==========================================// 5. toString 检查// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 5️⃣  toString 输出检查                                            │');console.log('└─────────────────────────────────────────────────────────────────┘');
    check(    'HTMLCollection.toString()',    spans.toString() === '[object HTMLCollection]',    `spans.toString() = "${spans.toString()}"`);
    check(    'HTMLSpanElement.toString()',    spans[0].toString() === '[object HTMLSpanElement]',    `spans[0].toString() = "${spans[0].toString()}"`);
    const toStringCode = spans[0].toString.toString();check(    'Function.toString 保护',    toStringCode.includes('[native code]'),    `包含 [native code] 标识`);
    // ==========================================// 6. document.all 反检测（最关键）// ==========================================console.log('');console.log('┌─────────────────────────────────────────────────────────────────┐');console.log('│ 6️⃣  document.all 反检测（最关键的检测点）                        │');console.log('└─────────────────────────────────────────────────────────────────┘');
    check(    'typeof document.all === "undefined"',    typeof document.all === 'undefined',    `typeof document.all = "${typeof document.all}"`);
    check(    'document.all == undefined',    document.all == undefined,    `document.all == undefined = true`);
    check(    'Boolean(document.all) === false',    Boolean(document.all) === false,    `Boolean(document.all) = false`);
    let ifExecuted = false;if (document.all) {    ifExecuted = true;}check(    'if (document.all) 不执行',    !ifExecuted,    `if 语句未执行（正确行为）`);
    // document.all 作为函数调用try {    const elem = document.all('app');    check(        'document.all("id") 可调用',        elem && elem.id === 'app',        `成功获取元素: id="app"`    );} catch (e) {    check('document.all("id") 可调用', false, `❌ 调用失败: ${e.message}`);}
    // document.all 作为数组访问try {    const firstElem = document.all[0];    check(        'document.all[index] 可访问',        firstElem && firstElem.tagName,        `成功访问: tagName="${firstElem?.tagName}"`    );} catch (e) {    check('document.all[index] 可访问', false, `❌ 访问失败: ${e.message}`);}
    // ==========================================// 测试结果汇总// ==========================================const totalTests = passCount + failCount;const passRate = ((passCount / totalTests) * 100).toFixed(1);
    console.log('');console.log('╔═══════════════════════════════════════════════════════════════════╗');console.log('║                          测试结果汇总                              ║');console.log('╠═══════════════════════════════════════════════════════════════════╣');console.log(`║  ✅ 通过: ${passCount.toString().padEnd(2)} 项                                                    ║`);console.log(`║  ❌ 失败: ${failCount.toString().padEnd(2)} 项                                                    ║`);console.log(`║  📊 通过率: ${passRate}%                                                   ║`);console.log('╠═══════════════════════════════════════════════════════════════════╣');

结果：


    ╔═══════════════════════════════════════════════════════════════════╗║                          测试结果汇总                              ║╠═══════════════════════════════════════════════════════════════════╣║  ✅ 通过: 22 项                                                    ║║  ❌ 失败: 0  项                                                    ║║  📊 通过率: 100.0%                                                   ║


目前的算是都过了。后面会不断补充。

这里对这个框架做一个临时性进度总结：

###  ✅ 已实现功能

  * ** JSON 配置导入与初始化  **
  * 实现了toSting保护，原型链保护，get/set保护（识别到函数是在构造 ,原型,或者实例上 ）
  * 分发函数优先用自己的envFuncs， 没有的话降级jsdom
  * 分发函数返回后c层自动包装+保护 ，业务层不用管

###  ✅ 待实现功能

  * **想实现对象的in，delete 等的js层拦截， 但是挂载在对象上的拦截器返回的是kYes 或者kNo，不是true或者false，暂时还没更好的思路。只输出了一行日志： **  std::cout << "[KhBox] IN called: " << dispatchName << std::endl;


      NamedPropertyHandlerConfiguration handler_config(        nullptr,        nullptr,        TemplateNamedPropertyQuery,        TemplateNamedPropertyDeleter,        TemplateNamedPropertyEnumerator,        TemplateNamedPropertyDefiner,        TemplateNamedPropertyDescriptor,        Local<Value>(),        PropertyHandlerFlags::kNonMasking    );    tpl->InstanceTemplate()->SetHandler(handler_config);

  * ** 错误堆栈自定义输出修改及filter  **
  * ** 运行时指定/动态指纹功能  ** **
