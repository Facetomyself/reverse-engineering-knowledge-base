# khbox补环境案例-2：yrx2内存爆破分析

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2026-04-02
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 先让ai还原一下 这个是一个普通的ob混淆， 给ai说基于我们的ast框架，先把十六进制还原一下，完成后再看下一步干什么。 然后： 他就直接写完了 还原完的结果如下： 看起来还是一个时间戳加密。但是内置了几个正则格式化判断还有内存爆破。 能看出来运行完后，document.cookie会自己加上m的cookie值。 当然，既然要补环境，我们就不去直接拿算法了，去试下能否通过hook的方式，让它内存爆破失效。 反调试bypass 那么针对…

## 正文

##  先让ai还原一下

这个是一个普通的ob混淆，


给ai说基于我们的ast框架，先把十六进制还原一下，完成后再看下一步干什么。

然后：

他就直接写完了


还原完的结果如下：

看起来还是一个时间戳加密。但是内置了几个正则格式化判断还有内存爆破。

能看出来运行完后，document.cookie会自己加上m的cookie值。

当然，既然要补环境，我们就不去直接拿算法了，去试下能否通过hook的方式，让它内存爆破失效。

##  反调试bypass

那么针对这个反调试，我们能做哪些事情呢？ claude给了个油猴脚本。仅供参考。


      // ==UserScript==  // @name         yrx2 反调试 Bypass  // @namespace    http://tampermonkey.net/// @match        https://match.yuanrenxue.cn/match/2  // @run-at       document-start  // @grant        none  // ==/UserScript==  (function () {      'use strict';
          // ══════════════════════════════════════════════════      // ① 保护 console — 拦截 eval 里的覆盖赋值      //    eval 代码做的是 `console = new Object()`      //    用 defineProperty 把 setter 设成黑洞      // ══════════════════════════════════════════════════      const _realConsole = window.console;      try {          Object.defineProperty(window, 'console', {              get() { return _realConsole; },              set(v) { /* 丢弃 */ },              configurable: true,          });      } catch (e) {}
          // ══════════════════════════════════════════════════      // ② Hook eval — 拦截 qz 数组的 console 炸弹      //    特征：含 pushState + while(1) + console      // ══════════════════════════════════════════════════      const _realEval = window.eval;      window.eval = function (code) {          if (typeof code === 'string'              && code.includes('pushState')              && code.includes('console')) {              console.log('[bypass] 已拦截 console 炸弹 eval ✓');              return undefined;          }          return _realEval.call(window, code);      };
          // ══════════════════════════════════════════════════      // ③ Hook Function 构造器 — 拦截动态 debugger 陷阱      //    Function("debugger").call(...)      //    Function("while (true) {}").apply(...)      // ══════════════════════════════════════════════════      const _realFunction = window.Function;      const _blockedBodies = [          /^\s*debugger\s*;?\s*$/,          /^\s*while\s*\(\s*(true|1)\s*\)\s*\{?\s*\}?\s*$/,      ];
          const _hookedFunction = new Proxy(_realFunction, {          apply(target, thisArg, args) {              const body = String(args[args.length - 1] ?? '');              if (_blockedBodies.some(r => r.test(body))) {                  console.log('[bypass] 拦截 Function("' + body.trim() + '") ✓');                  return function () {};        // 返回空函数代替              }              return Reflect.apply(target, thisArg, args);          },          construct(target, args) {              const body = String(args[args.length - 1] ?? '');              if (_blockedBodies.some(r => r.test(body))) {                  console.log('[bypass] 拦截 new Function("' + body.trim() + '") ✓');                  return function () {};              }              return Reflect.construct(target, args);          },      });
          try {          Object.defineProperty(window, 'Function', {              value: _hookedFunction,              writable: true, configurable: true,          });      } catch (e) {}
          // ══════════════════════════════════════════════════      // ④ Hook RegExp.prototype.compile — 干掉 ReDoS      //    pattern: ^([^ ]+( +[^ ]+)+)+[^ ]}      //    把它换成 ^$ (永不匹配)，test() 快速返回 false      //    让 !test() === true，检查直接通过      // ══════════════════════════════════════════════════      const _realCompile = RegExp.prototype.compile;      RegExp.prototype.compile = function (pattern, flags) {          if (typeof pattern === 'string' && pattern.includes('( +[^ ]+)+')) {              console.log('[bypass] 拦截 ReDoS compile ✓');              return _realCompile.call(this, '^$', flags);  // 无害替代          }          return _realCompile.call(this, pattern, flags);      };
          // ══════════════════════════════════════════════════      // ⑤ Hook history.pushState — 兜底      //    万一 eval 没被拦截，至少让 pushState 变 no-op      //    避免浏览器历史记录爆炸      // ══════════════════════════════════════════════════      const _realPushState = history.pushState.bind(history);      let _pushStateCount = 0;      history.pushState = function (...args) {          if (++_pushStateCount > 100) {              // 超过100次认为是炸弹，后续全部忽略              if (_pushStateCount === 101)                  console.log('[bypass] pushState 炸弹已限速 ✓');              return;          }          return _realPushState(...args);      };
          console.log('[bypass] 所有反调试钩子已安装 ✓');  })();

###  khbox fix

让ai粗略生成了一个hook ，以便覆盖我们之前的框架hook,位于加载input.js之前。


    // pages/yrx2/preload.js// 针对 yrx2 反调试陷阱的定向 patch，不修改 input.js 和 envFuncs// 执行时机：jQuery 注入之后、input.js 之前
    // ─── 坑1：ReDoS 灾难性回溯 ──────────────────────────────────────────────────// 陷阱：_0x59ce03["constructor"]("return /.../")().compile("^([^ ]+( +[^ ]+)+)+[^ ]}")//       把正则替换成指数级回溯版本，再拿函数源码去 test → CPU 死循环// Fix：让 compile() 变成 no-op，正则保持原始状态RegExp.prototype.compile = function() { return this; };
    // ─── 坑2：eval 替换 console.log → while(1) 死循环 ────────────────────────────// 陷阱：eval 的代码把 console.log 替换为内含 while(1){ history.pushState×11万 } 的函数//       JSDOM 里 history.pushState 会成功 → while(1) 真的死循环// Fix：通过 _khGetJsdom() 直接改 JSDOM impl 的 pushState（shadow 层优先级不够）//       pushState 抛异常 → for 循环 → 异常逃出 while(1) → console.log 调用立即返回try {    var _jsdomWin = _khGetJsdom();    if (_jsdomWin && _jsdomWin.history) {        _jsdomWin.history.pushState = function() {            throw new ReferenceError('history.pushState blocked by preload');        };    }} catch(e) {}
    // ─── 坑3：setInterval/setTimeout 防止周期性重触发 ────────────────────────────// 注意：setInterval(_0x5e4b0d(), 500) 里 _0x5e4b0d() 已同步执行（有括号）//       坑1/坑2 修好后同步执行无害，这里只是防止返回值被再次注册为定时器var _timerNoop = function() { return 0; };window.setInterval = _timerNoop;window.setTimeout  = _timerNoop;


input.js如下：


原封不动的放进去。


有用。

理论上来说，到这里就可以很快结束了，只要像第一个demo一样，获取到cookie 发起更新就行了

**但是，我又把框架改崩了 **。这个截图仅此一份，种种改动都成云烟。

##  分析一下爆破点

###  正则爆破


    var _0xddb775 = function () {    var _0x24cd5c = true;    return function (_0x5689ed, _0x89aab9) {        var _0x3f5dea = _0x24cd5c ? function () {            if (_0x89aab9) {                var _0x333d2c = _0x89aab9["apply"](_0x5689ed, arguments);                _0x89aab9 = null;                return _0x333d2c;            }        } : function () {};        _0x24cd5c = false;        return _0x3f5dea;    };}();
    var _0x3490f5 = _0xddb775(this, function () {    var _0x16d0c2 = function () {        var _0xd5a44d = _0x16d0c2["constructor"]("return /\" + this + \"/")()["compile"]("^([^ ]+( +[^ ]+)+)+[^ ]}");        return !_0xd5a44d["test"](_0x3490f5);    };    return _0x16d0c2();});_0x3490f5();
    这是一个典型的 ReDoS（正则表达式拒绝服务） 模式。([^ ]+( +[^ ]+)+)+ 包含了嵌套的循环和重复。当目标字符串（即代码函数本身）是压缩成一行的（Minified）时，正则匹配会很快失败或结束。但是，如果你格式化了代码，代码中会出现大量的空格和换行符。当正则表达式在匹配这些带有复杂空格结构的字符串时，如果匹配不成功，引擎会尝试进行指数级次数的回溯（Backtracking）。后果：CPU 使用率会立即飙升到 100%，浏览器页面或 Node.js 进程会失去响应，看起来就像程序“死锁”了。
    当然，我们不格式化就行

###  console无限输出


    ounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(line  eval(_0x2ceba9(qz));    try {      if (global) {        console["log"]("人生苦短，何必python？");      } else {        while (1) {          console["log"]("人生苦短，何必python？");        }      }    } catch (_0x10ab28) {      return navigator["vendorSub"];    }//通过hook evla，能知道他是给console重新赋值，只要调用了console.log直接无限输出，然后通过检测globa 触发// hook eval失效， 不让console被重写，或者直接冻结consoel //删掉global

###  正则检测

while 1 和debugger


    ounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(line   (function () {            _0x52c015(this, function () {                var _0x560737 = new RegExp("function *\\( *\\)");                var _0x29c009 = new RegExp("\\+\\+ *(?:[a-zA-Z_$][0-9a-zA-Z_$]*)", "i");                var _0x235080 = $dbsm_0x11a00a("init");                if (!_0x560737["test"](_0x235080 + "chain") || !_0x29c009["test"](_0x235080 + "input")) {                    _0x235080("0");                } else {                    $dbsm_0x11a00a();                }            })();        })();
    function $dbsm_0x11a00a(_0x4d4747) {  function _0x28ed65(_0x5df4e4) {    if (typeof _0x5df4e4 === "string") {      return function (_0x156894) {}["constructor"]("while (true) {}")["apply"]("counter");    } else {      if (('' + _0x5df4e4 / _0x5df4e4)["length"] !== 1 || _0x5df4e4 % 20 === 0) {        (function () {          return true;        })["constructor"]("debugger")["call"]("action");      } else {        (function () {          return false;        })["constructor"]("debugger")["apply"]("stateObject");      }    }    _0x28ed65(++_0x5df4e4);  }  try {    if (_0x4d4747) {      return _0x28ed65;    } else {      _0x28ed65(0);    }  } catch (_0xe1517f) {}}
    // 加强版 _0x235080 + "chain" 自动触发函数的toString  // 只要进了 _0x235080("0"); ，有传参的话就完了， 字符串直接无限循环while 卡死，// 非字符串 就无限debugger ;  _0x28ed65(0); 但是不开f12 就没有影响。
    //所以这段也是，不格式化没事。

###  setInterval

还有最后一个地方


    ounter(lineounter(lineounter(lineounter(lineounter(lineounter(line  setInterval(_0x4ede3d(), 500);  ......setInterval(function () {  $dbsm_0x11a00a();}, 4000);//已经知道了这两个里面是检测，那就直接把setInterval置空

最后运行一下


    "C:\Program Files\nodejs\node.exe" D:\Code\htmlCode\khbox_vm\khbox_node\pages\yrx2\input_ori.jsm=5549879ef737c7445aeeb70945d6f839|1774887160387; path=/


行了，重新fix中。
