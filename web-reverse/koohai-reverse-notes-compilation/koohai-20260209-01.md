# khbox-7：v1完善

> 来源: 微信公众号：零基础爬虫第一天
> 原始发布时间: 2026-02-09
> 归档日期: 2026-09-06
> 分类: web-reverse
>
> 目前这一版 这个input 终于过了，可能还有其他遗漏点后面再补。新加的几个监测点比如非法调用，stack堆栈等，虽然加好了但是添加的过程太麻烦了，所以接下来一方面准备从头梳理一个入门文档，从v8 基本函数操作，到这个框架的每一步组成添加功能重新做一个专题，加一点优化。另一方面准备一下新内容。大家可以把这个检测放自己的代码里试下： 模拟的html为： <!DOCTYPE html <html lang="en" <head <meta…

## 正文

目前这一版 这个input
终于过了，可能还有其他遗漏点后面再补。新加的几个监测点比如非法调用，stack堆栈等，虽然加好了但是添加的过程太麻烦了，所以接下来一方面准备从头梳理一个入门文档，从v8
基本函数操作，到这个框架的每一步组成添加功能重新做一个专题，加一点优化。另一方面准备一下新内容。大家可以把这个检测放自己的代码里试下：

模拟的html为：


    <!DOCTYPE html><html lang="en"><head>    <meta charset="UTF-8">    <title>Demo Page</title></head><body><div id="app" class="container">    <h1>Test Page</h1>    <p>这是一个测试页面</p></div>
    <div id="content">    <span id="span1">Span 1</span>    <span id="span2">Span 2</span></div>
    <input type="text" id="input1" value="test" /><button id="btn1">Click Me</button></body></html>

先上图：


input.js:


    // ============================================================================// 🛠️ KhBox 环境模拟深度检测套件 (Advanced Anti-Bot Verification)// ============================================================================
    // --- 🎨 基础工具与日志配置 ---const COLORS = {    RESET: "\x1b[0m",    PASS: "\x1b[32m", // Green    FAIL: "\x1b[31m", // Red    INFO: "\x1b[36m", // Cyan    WARN: "\x1b[33m", // Yellow    BOLD: "\x1b[1m",    GRAY: "\x1b[90m"};
    const STATS = { pass: 0, fail: 0 };
    function logHeader(title) {    console.log(`\n${COLORS.BOLD}${COLORS.INFO}┌──────────────────────────────────────────────────────────────┐${COLORS.RESET}`);    console.log(`${COLORS.BOLD}${COLORS.INFO}│ ${title.padEnd(60)} │${COLORS.RESET}`);    console.log(`${COLORS.BOLD}${COLORS.INFO}└──────────────────────────────────────────────────────────────┘${COLORS.RESET}`);}
    function check(name, condition, successMsg = '', failureMsg = '') {    if (condition) {        STATS.pass++;        console.log(` ${COLORS.PASS}✔ [PASS]${COLORS.RESET} ${name}`);        if (successMsg) console.log(`       ${COLORS.GRAY}└─ ${successMsg}${COLORS.RESET}`);    } else {        STATS.fail++;        console.log(` ${COLORS.FAIL}✖ [FAIL]${COLORS.RESET} ${name}`);        if (failureMsg) console.log(`       ${COLORS.FAIL}└─ 期望: ${failureMsg}${COLORS.RESET}`);    }}
    function safeRun(fn) {    try {        return { success: true, value: fn() };    } catch (e) {        return { success: false, error: e };    }}
    // ============================================================================// 🚀 测试开始// ============================================================================
    console.log(`${COLORS.BOLD}╔══════════════════════════════════════════════════════════════╗`);console.log(`║           🛡️ KhBox 环境模拟深度体检报告                      ║`);console.log(`╚══════════════════════════════════════════════════════════════╝${COLORS.RESET}`);
    // ----------------------------------------------------------------------------// 1️⃣ 基础全局环境与 Native 保护// ----------------------------------------------------------------------------logHeader("1️⃣  基础全局环境 & Native 保护");
    // 1.1 全局对象引用check('window === self', window === self, "window 和 self 引用一致");check('window === globalThis', window === globalThis, "window 和 globalThis 引用一致");check('window === top', window === top, "非 iframe 环境下 window === top");check('window === parent', window === parent, "非 iframe 环境下 window === parent");
    // 1.2 Native 函数 toString 保护const alertStr = window.alert.toString();check(    'alert.toString() 格式',    alertStr === 'function alert() { [native code] }',    `输出: ${alertStr}`,    `应为 "function alert() { [native code] }"`);
    check(    'Function.prototype.toString.call(alert) 保护',    Function.prototype.toString.call(window.alert) === 'function alert() { [native code] }',    "无法通过 call 绕过 toString 保护");
    // 1.3 构造函数检测check(    'navigator.constructor.name === "Navigator"',    navigator.constructor.name === 'Navigator',    `实际值: ${navigator.constructor.name}`);
    check(    'document.constructor.name === "HTMLDocument"',    document.constructor.name === 'HTMLDocument',    `实际值: ${document.constructor.name}`);
    check(    'location.constructor.name === "Location"',    location.constructor.name === 'Location',    `实际值: ${location.constructor.name}`);
    // ----------------------------------------------------------------------------// 2️⃣ 原型链完整性检测// ----------------------------------------------------------------------------logHeader("2️⃣  原型链完整性检测");
    // 2.1 HTMLDivElement 原型链const div = document.createElement('div');check(    'div.__proto__ === HTMLDivElement.prototype',    div.__proto__ === HTMLDivElement.prototype,    "第一层原型正确");check(    'HTMLDivElement.prototype.__proto__ === HTMLElement.prototype',    HTMLDivElement.prototype.__proto__ === HTMLElement.prototype,    "HTMLDivElement → HTMLElement");check(    'HTMLElement.prototype.__proto__ === Element.prototype',    HTMLElement.prototype.__proto__ === Element.prototype,    "HTMLElement → Element");check(    'Element.prototype.__proto__ === Node.prototype',    Element.prototype.__proto__ === Node.prototype,    "Element → Node");check(    'Node.prototype.__proto__ === EventTarget.prototype',    Node.prototype.__proto__ === EventTarget.prototype,    "Node → EventTarget");
    // 2.2 构造函数原型链check(    'HTMLDivElement.__proto__ === HTMLElement',    HTMLDivElement.__proto__ === HTMLElement,    "构造函数继承关系正确");check(    'HTMLElement.__proto__ === Element',    HTMLElement.__proto__ === Element,    "构造函数继承关系正确");
    // ----------------------------------------------------------------------------// 3️⃣ instanceof 检测// ----------------------------------------------------------------------------logHeader("3️⃣  instanceof 检测");
    check('div instanceof HTMLDivElement', div instanceof HTMLDivElement);check('div instanceof HTMLElement', div instanceof HTMLElement);check('div instanceof Element', div instanceof Element);check('div instanceof Node', div instanceof Node);check('div instanceof EventTarget', div instanceof EventTarget);check('div instanceof Object', div instanceof Object);
    check('document instanceof HTMLDocument', document instanceof HTMLDocument);check('document instanceof Document', document instanceof Document);check('document instanceof Node', document instanceof Node);
    check('navigator instanceof Navigator', navigator instanceof Navigator);check('location instanceof Location', location instanceof Location);check('history instanceof History', history instanceof History);
    // 集合类型const spans = document.getElementsByTagName('span');if (spans.length > 0) {    check('HTMLCollection instanceof', spans instanceof HTMLCollection);    check('spans[0] instanceof HTMLSpanElement', spans[0] instanceof HTMLSpanElement);}
    const nodeList = document.querySelectorAll('div');check('NodeList instanceof', nodeList instanceof NodeList);
    // ----------------------------------------------------------------------------// 4️⃣ toString / Symbol.toStringTag 检测// ----------------------------------------------------------------------------logHeader("4️⃣  toString & Symbol.toStringTag 检测");console.log('Object.prototype.toString.call(window)=========',Object.prototype.toString.call(window))check(    'Object.prototype.toString.call(window)',    Object.prototype.toString.call(window) === '[object Window]',    `实际: ${Object.prototype.toString.call(window)}`);check(    'Object.prototype.toString.call(document)',    Object.prototype.toString.call(document) === '[object HTMLDocument]',    `实际: ${Object.prototype.toString.call(document)}`);check(    'Object.prototype.toString.call(navigator)',    Object.prototype.toString.call(navigator) === '[object Navigator]',    `实际: ${Object.prototype.toString.call(navigator)}`);check(    'Object.prototype.toString.call(location)',    Object.prototype.toString.call(location) === '[object Location]',    `实际: ${Object.prototype.toString.call(location)}`);check(    'Object.prototype.toString.call(div)',    Object.prototype.toString.call(div) === '[object HTMLDivElement]',    `实际: ${Object.prototype.toString.call(div)}`);
    // 构造函数的 toStringcheck(    'HTMLDivElement.toString()',    HTMLDivElement.toString() === 'function HTMLDivElement() { [native code] }',    `实际: ${HTMLDivElement.toString()}`);check(    'Element.toString()',    Element.toString() === 'function Element() { [native code] }',    `实际: ${Element.toString()}`);
    // ----------------------------------------------------------------------------// 5️⃣ document.all 特殊检测（最关键）// ----------------------------------------------------------------------------logHeader("5️⃣  document.all 特殊检测（反检测关键点）");
    check(    'typeof document.all === "undefined"',    typeof document.all === 'undefined',    `实际 typeof: ${typeof document.all}`);check(    'document.all == null',    document.all == null,    "Loose equality with null");check(    'document.all == undefined',    document.all == undefined,    "Loose equality with undefined");check(    'Boolean(document.all) === false',    Boolean(document.all) === false,    `实际: ${Boolean(document.all)}`);check(    '!document.all === true',    !document.all === true,    "取反为 true");
    // if 判断不执行let ifExecuted = false;if (document.all) { ifExecuted = true; }check('if (document.all) 不执行', !ifExecuted, "if 语句未执行");
    // 但它确实存在并可调用const allResult = safeRun(() => document.all('app'));check(    'document.all("id") 可调用',    allResult.success,    allResult.success ? `获取到元素` : `调用失败: ${allResult.error?.message}`);
    const allIndexResult = safeRun(() => document.all[0]);check(    'document.all[0] 可索引访问',    allIndexResult.success && allIndexResult.value !== undefined,    allIndexResult.success ? `tagName: ${allIndexResult.value?.tagName}` : `访问失败`);
    check(    'document.all.constructor.name === "HTMLAllCollection"',    document.all.constructor.name === 'HTMLAllCollection',    `实际: ${document.all.constructor?.name}`);
    // ----------------------------------------------------------------------------// 6️⃣ 属性描述符与枚举检测// ----------------------------------------------------------------------------logHeader("6️⃣  属性描述符与枚举检测");
    // webdriver 属性检测check(    'navigator.webdriver === false 或 undefined',    navigator.webdriver === false || navigator.webdriver === undefined,    `实际值: ${navigator.webdriver}`);
    check(    'Object.getOwnPropertyDescriptor(navigator, "webdriver") 为 undefined',    Object.getOwnPropertyDescriptor(navigator, 'webdriver') === undefined,    "webdriver 不在实例上");console.log('=============start ')// 获取 window 的原型 (即 Window.prototype)const proto = Object.getPrototypeOf(window);// window.navigator 描述符const navDesc = Object.getOwnPropertyDescriptor(window, 'navigator');// // 优先查实例，如果没有查原型// const navDesc = Object.getOwnPropertyDescriptor(window, 'navigator') ||//     Object.getOwnPropertyDescriptor(proto, 'navigator');
    console.log("navDesc exists:", !!navDesc);
    // 安全的检查逻辑if (navDesc) {    console.log("typeof navDesc.get === 'function' ======== ", typeof navDesc.get === 'function');} else {    console.log("window.navigator 描述符不存在");}
    check(    'window.navigator 有 getter',    navDesc && typeof navDesc.get === 'function',    navDesc ? `有 getter` : `无描述符`);


    console.log("typeof navDesc.get === 'function' ======== " ,typeof navDesc.get === 'function')check(    'window.navigator 有 getter',    navDesc && typeof navDesc.get === 'function',    navDesc ? `有 getter` : `无描述符`);
    // Getter 的 toString 保护if (navDesc && navDesc.get) {    const getterStr = navDesc.get.toString();    check(        'navigator getter toString 为 native',        getterStr.includes('[native code]'),        `实际: ${getterStr.substring(0, 50)}...`    );}
    // for...in 不枚举内部属性const navKeys = [];for (const k in navigator) { navKeys.push(k); }

    // ----------------------------------------------------------------------------// 7️⃣ Illegal Invocation 保护// ----------------------------------------------------------------------------logHeader("7️⃣  Illegal Invocation 保护");
    // 方法脱离上下文调用应该抛出 Illegal invocationconst getElementByIdRef = document.getElementById;const illegalResult = safeRun(() => getElementByIdRef('app'));check(    'getElementById 脱离上下文抛出 Illegal invocation',    !illegalResult.success && illegalResult.error?.message?.includes('Illegal invocation'),    illegalResult.success ? `意外成功` : `错误: ${illegalResult.error?.message}`);
    const setAttributeRef = div.setAttribute;const illegalResult2 = safeRun(() => setAttributeRef('id', 'test'));check(    'setAttribute 脱离上下文抛出 Illegal invocation',    !illegalResult2.success && illegalResult2.error?.message?.includes('Illegal invocation'),    illegalResult2.success ? `意外成功` : `错误: ${illegalResult2.error?.message}`);
    // 使用 call 改变 this 也应该抛出const illegalResult3 = safeRun(() => document.getElementById.call(null, 'app'));check(    'getElementById.call(null) 抛出错误',    !illegalResult3.success,    illegalResult3.success ? `意外成功` : `正确抛出错误`);
    // 正确使用 bind 应该可以const boundGetById = document.getElementById.bind(document);const bindResult = safeRun(() => boundGetById('app'));check(    'getElementById.bind(document) 正常工作',    bindResult.success,    bindResult.success ? `bind 后调用成功` : `失败: ${bindResult.error?.message}`);
    // ----------------------------------------------------------------------------// 8️⃣ DOM 操作与同步检测// ----------------------------------------------------------------------------logHeader("8️⃣  DOM 操作与同步检测");
    // 创建元素const testDiv = document.createElement('div');testDiv.id = 'khbox-test';testDiv.className = 'test-class';testDiv.setAttribute('data-custom', 'value');
    check('createElement 返回正确类型', testDiv instanceof HTMLDivElement);check('id 赋值生效', testDiv.id === 'khbox-test', `实际: ${testDiv.id}`);check('className 赋值生效', testDiv.className === 'test-class', `实际: ${testDiv.className}`);check('getAttribute 正常', testDiv.getAttribute('data-custom') === 'value');
    // innerHTML 操作testDiv.innerHTML = '<span>test</span>';check('innerHTML 赋值生效', testDiv.innerHTML === '<span>test</span>', `实际: ${testDiv.innerHTML}`);check('children 更新', testDiv.children.length === 1);check('firstElementChild 正确', testDiv.firstElementChild?.tagName === 'SPAN');
    // classList 操作testDiv.classList.add('added-class');check('classList.add 生效', testDiv.classList.contains('added-class'));testDiv.classList.remove('test-class');check('classList.remove 生效', !testDiv.classList.contains('test-class'));
    // ----------------------------------------------------------------------------// 9️⃣ Error.stack 检测// ----------------------------------------------------------------------------logHeader("9️⃣  Error.stack 检测");
    const testError = new Error('test error');console.log("typeof testError.stack===",typeof testError.stack)check(    'Error.stack 存在',    typeof testError.stack === 'string',    `stack 类型: ${typeof testError.stack}`);
    // 堆栈中不应该暴露 khbox 内部路径const stackStr = testError.stack || '';console.log('testError.stack == ',testError.stack)check(    'Error.stack 不暴露 khbox_internal',    !stackStr.includes('khbox_internal'),    stackStr.includes('khbox_internal') ? '暴露了内部路径' : '堆栈干净');check(    'Error.stack 不暴露 node_modules',    !stackStr.includes('node_modules'),    stackStr.includes('node_modules') ? '暴露了 node_modules' : '堆栈干净');
    // ----------------------------------------------------------------------------// 🔟 特殊对象与边界检测// ----------------------------------------------------------------------------logHeader("🔟  特殊对象与边界检测");
    // Navigator 特殊属性check(    'navigator.userAgent 是字符串',    typeof navigator.userAgent === 'string',    `实际: ${navigator.userAgent?.substring(0, 50)}...`);check(    'navigator.platform 是字符串',    typeof navigator.platform === 'string',    `实际: ${navigator.platform}`);check(    'navigator.language 是字符串',    typeof navigator.language === 'string',    `实际: ${navigator.language}`);check(    'navigator.languages 是数组',    Array.isArray(navigator.languages),    `实际类型: ${typeof navigator.languages}`);
    // plugins 和 mimeTypes（常用检测点）check(    'navigator.plugins 存在',    navigator.plugins !== undefined,    `类型: ${typeof navigator.plugins}`);check(    'navigator.mimeTypes 存在',    navigator.mimeTypes !== undefined,    `类型: ${typeof navigator.mimeTypes}`);
    // Screen 对象check('screen.width 是数字', typeof screen.width === 'number', `实际: ${screen.width}`);check('screen.height 是数字', typeof screen.height === 'number', `实际: ${screen.height}`);check('screen.colorDepth 是数字', typeof screen.colorDepth === 'number', `实际: ${screen.colorDepth}`);
    // Location 对象check('location.href 是字符串', typeof location.href === 'string', `实际: ${location.href}`);check('location.protocol 是字符串', typeof location.protocol === 'string', `实际: ${location.protocol}`);
    // History 对象check('history.length 是数字', typeof history.length === 'number', `实际: ${history.length}`);
    // Document 特殊属性check('document.cookie 是字符串', typeof document.cookie === 'string');check('document.readyState 是字符串', typeof document.readyState === 'string', `实际: ${document.readyState}`);check('document.documentElement 存在', document.documentElement instanceof HTMLHtmlElement);check('document.body 存在', document.body instanceof HTMLBodyElement);check('document.head 存在', document.head instanceof HTMLHeadElement);
    // ----------------------------------------------------------------------------// 1️⃣1️⃣ 高级原型链攻击检测// ----------------------------------------------------------------------------logHeader("1️⃣1️⃣  高级原型链攻击检测");
    // Object.getPrototypeOf 一致性check(    'Object.getPrototypeOf(div) === HTMLDivElement.prototype',    Object.getPrototypeOf(div) === HTMLDivElement.prototype,    "getPrototypeOf 和 __proto__ 一致");
    // Reflect.getPrototypeOf 一致性check(    'Reflect.getPrototypeOf(div) === HTMLDivElement.prototype',    Reflect.getPrototypeOf(div) === HTMLDivElement.prototype,    "Reflect API 一致");
    // constructor 链完整check('div.constructor === HTMLDivElement', div.constructor === HTMLDivElement);check('HTMLDivElement.prototype.constructor === HTMLDivElement', HTMLDivElement.prototype.constructor === HTMLDivElement);
    // Symbol.hasInstance 检测check(    'HTMLDivElement[Symbol.hasInstance](div)',    HTMLDivElement[Symbol.hasInstance](div),    "Symbol.hasInstance 正常工作");
    // Object.prototype.isPrototypeOfcheck(    'HTMLDivElement.prototype.isPrototypeOf(div)',    HTMLDivElement.prototype.isPrototypeOf(div),    "isPrototypeOf 正常工作");check(    'HTMLElement.prototype.isPrototypeOf(div)',    HTMLElement.prototype.isPrototypeOf(div),    "继承链 isPrototypeOf 正确");
    // ----------------------------------------------------------------------------// 1️⃣2️⃣ 时间与性能检测// ----------------------------------------------------------------------------logHeader("1️⃣2️⃣  时间与性能检测");
    check('Date.now() 返回数字', typeof Date.now() === 'number', `实际: ${Date.now()}`);check('new Date() 有效', new Date() instanceof Date);
    if (typeof performance !== 'undefined') {    check('performance.now() 返回数字', typeof performance.now() === 'number', `实际: ${performance.now()}`);    check('performance.timing 存在', performance.timing !== undefined);}
    // ============================================================================// 📊 总结// ============================================================================console.log(`\n${COLORS.BOLD}╔══════════════════════════════════════════════════════════════╗`);const rate = ((STATS.pass / (STATS.pass + STATS.fail)) * 100).toFixed(1);const color = STATS.fail === 0 ? COLORS.PASS : (STATS.fail < 5 ? COLORS.WARN : COLORS.FAIL);
    console.log(`║  ✅ 通过: ${STATS.pass.toString().padEnd(3)}   ❌ 失败: ${STATS.fail.toString().padEnd(3)}   📊 得分: ${color}${rate}%${COLORS.RESET}${COLORS.BOLD}             ║`);console.log(`╚══════════════════════════════════════════════════════════════╝${COLORS.RESET}\n`);
    if (STATS.fail === 0) {    console.log(`${COLORS.PASS}🎉  完美！环境模拟程度极高，可以绕过大部分反爬虫检测。${COLORS.RESET}`);} else if (STATS.fail < 5) {    console.log(`${COLORS.WARN}⚠️  大部分测试通过，少数问题需要修复。${COLORS.RESET}`);} else {    console.log(`${COLORS.FAIL}❌  检测到多处特征泄漏，建议优先修复 [FAIL] 项目。${COLORS.RESET}`);}


但是在增加监测点的时候，也发现了一些隐患，部分功能还是在js实现的，比如这个初始化的js


    ounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(lineounter(line
    //khbox_bootstrap.js(function(){    console.log('[VM Context] 开始初始化...');
        // ✅ 在 VM Context 中初始化（为这个 Context 创建 Template 和 patch ctor）    addon.initializeInContext(__config);    console.log('[VM Context] Template 创建完成');
        // ✅ 这些调用发生在 VM context 中，所以创建的对象原型链在 VM context    const shadowWindow = addon.createInstance(globalThis,"Window");    const shadowDoc = addon.createInstance(globalThis,"HTMLDocument");    const shadowNavigator = addon.createInstance(globalThis,"Navigator");    const shadowLocation = addon.createInstance(globalThis,"Location");    const shadowHistory = addon.createInstance(globalThis,"History");    const shadowScreen = addon.createInstance(globalThis,"Screen");    const shadowPerformance = addon.createInstance(globalThis,"Performance");
        console.log('[VM Context] Shadow 对象创建完成');    const shadowEventTarget = addon.createInstance(globalThis,"EventTarget");    console.log('shadowEventTarget', shadowEventTarget);    console.log('shadowEventTarget instanceof Object  ', shadowEventTarget instanceof Object );    // ✅ 绑定 JSDOM 实现    const w = __jsdomWindow;    addon.setImpl(shadowWindow, w);    addon.setImpl(shadowDoc, w.document);    addon.setImpl(shadowNavigator, w.navigator);    addon.setImpl(shadowLocation, w.location);    addon.setImpl(shadowHistory, w.history);    addon.setImpl(shadowScreen, w.screen);    addon.setImpl(shadowPerformance, w.performance);    // addon.setImpl(shadowScreen, w.screen);    console.log('[VM Context] JSDOM 实现已绑定');
        // ✅ 构造函数挂全局    const nativeConstructors = addon.getConstructors(globalThis);    console.log("nativeConstructors  ===",nativeConstructors.toString())    Object.assign(globalThis, nativeConstructors);    console.log('[VM Context] 构造函数已挂载到全局');    // 解决window.alert 的问题
        // ✅ 全局别名// ✅ 全局别名 — 用 accessor 保证引用一致    for (const k of ['window','self','top','parent']) {        Object.defineProperty(globalThis, k, {            get(){ return globalThis; },            configurable: true,            enumerable: true        });    }
        const instanceProps = ['navigator','document','location','history','screen','performance'];
        for (const propName of instanceProps) {        const desc = Object.getOwnPropertyDescriptor(shadowWindow, propName);        if (desc && (desc.get || desc.set)) {            // 直接复制描述符（get/set 函数已经被 C++ 保护过了）            Object.defineProperty(globalThis, propName, desc);        }    }    console.log('[VM Context] Window 实例属性已镜像到 globalThis');    // instanceProps=['navigator','document','location','history','screen','performance']    const shadowDesc = Object.getOwnPropertyDescriptors(shadowWindow);    for (const [key, desc] of Object.entries(shadowDesc)) {        // 跳过系统属性和已存在的属性        if (key === 'constructor' || key === '__proto__') continue;        if (key in globalThis) continue;        if (instanceProps.includes(key)) continue;        try {            // console.log("setting attr to  global ",key )
                Object.defineProperty(globalThis, key, desc);        } catch(e) {            // 部分属性可能无法定义，忽略        }    }    console.log('[VM Context] Window 实例属性已镜像到 globalThis');
        Object.setPrototypeOf(globalThis, shadowWindow);
        // 2. 修复 toString 检测    Object.defineProperty(globalThis, Symbol.toStringTag, {        value: 'Window',        writable: false,        enumerable: false,        configurable: true    });


        globalThis.__shadowRefs = {        window: shadowWindow,        document: shadowDoc,        navigator: shadowNavigator,        location: shadowLocation,        history: shadowHistory,        screen: shadowScreen,        performance: shadowPerformance,    };
        console.log('[VM Context] 全局对象设置完成');
        // 清理临时变量    delete globalThis.__jsdomWindow;    delete globalThis.__config;})();


代码量还是有点大，部分还是放在了js层来做保护，需要重构。ai写的代码债，还是要还的。
